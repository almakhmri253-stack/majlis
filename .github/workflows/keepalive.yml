name: Keep Alive

on:
  schedule:
    - cron: '*/25 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Hugging Face Space
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://azizproject-majlis.hf.space
          echo " - Space is awake"
